// Browser JavaScript runs inside a web browser and interacts with HTML and the DOM.
// Node.js runs JavaScript outside the browser on the server or terminal and does not require HTML files.

// REPL stands for Read-Eval-Print Loop.
// It is an interactive environment where you can quickly test JavaScript code,
// debug expressions, and experiment without creating a separate file.
const message = "Hello from Node.js";
console.log(message);

const fruits = ["apple", "banana", "mango"];
const upper = fruits.map((f) => f.toUpperCase());
console.log(upper);
